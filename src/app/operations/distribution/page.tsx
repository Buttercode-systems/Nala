import Link from "next/link";
import {ArrowLeft,BriefcaseBusiness,Building2,Repeat2,ShieldCheck,UsersRound} from "lucide-react";
import {getDistributionReadiness} from "@/lib/distribution-data";

export const dynamic="force-dynamic";

export default async function DistributionOperationsPage(){const {rows,source,error}=await getDistributionReadiness();return <main className="min-h-screen bg-[#f3efe6] px-4 py-5 text-[#12211a] md:px-8 md:py-8"><div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-center justify-between gap-3"><Link href="/" className="inline-flex items-center gap-2 text-sm font-bold"><ArrowLeft size={16}/>Back to Nala</Link><span className="rounded-full bg-white px-3 py-2 text-xs font-extrabold">Operational readiness · {source}{error?" · live source unavailable":""}</span></div><header className="mt-7 rounded-[30px] bg-[#0d2b20] p-6 text-white md:p-10"><p className="text-xs font-black tracking-[.16em] text-[#dff09f]">CELL-SCALABLE OPERATIONS</p><h1 className="mt-3 max-w-4xl font-serif text-4xl leading-tight md:text-6xl">Concentrate demand, protect liquidity, then expand.</h1><p className="mt-4 max-w-3xl leading-7 text-white/70">This dashboard exposes the operating controls Nala uses before broader worker acquisition: task supply, verified businesses, recurring work, partner cohorts and capacity.</p></header><section className="mt-6 grid gap-5">{rows.length?rows.map(row=><article key={row.market_cell_id} className="rounded-[28px] border border-black/10 bg-white p-6 md:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.14em] text-[#9a4d31]">{row.status}</p><h2 className="mt-2 text-3xl font-black">{row.market_cell_name}</h2><p className="mt-2 text-sm text-black/55">{row.geography} · Last reviewed {row.last_reviewed_at?new Date(row.last_reviewed_at).toLocaleString("en-ZA"):"pending"}</p></div><ShieldCheck className="text-[#1d513b]" size={34}/></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Metric icon={BriefcaseBusiness} label="Funded open tasks" value={row.funded_open_task_count}/><Metric icon={UsersRound} label="Active workers" value={`${row.active_worker_count}/${row.worker_capacity}`}/><Metric icon={Building2} label="Verified businesses" value={row.verified_businesses}/><Metric icon={Repeat2} label="Recurring plans" value={row.recurring_plans}/><Metric icon={ShieldCheck} label="Task products" value={row.active_task_products}/></div><div className="mt-5 rounded-2xl bg-[#fff6df] p-4 text-sm font-bold">Partner cohorts active: {row.active_partner_cohorts}. Worker acquisition should remain controlled until funded task supply and repeat business justify expansion.</div></article>):<article className="rounded-[28px] bg-white p-8"><h2 className="text-2xl font-black">No distribution cell is publicly reportable yet.</h2><p className="mt-2 text-black/55">Nala will not manufacture marketplace health metrics.</p></article>}</section><section className="mt-6 grid gap-4 md:grid-cols-3"><Rule title="Supply gate" body="No broad worker acquisition before credible funded task volume exists."/><Rule title="Funding gate" body="No paid task reaches workers before funding is confirmed."/><Rule title="Expansion gate" body="Do not expand category and geography simultaneously."/></section></div></main>}

function Metric({icon:Icon,label,value}:{icon:any;label:string;value:string|number}){return <div className="rounded-2xl bg-[#f3efe6] p-4"><Icon size={18}/><strong className="mt-3 block text-2xl">{value}</strong><span className="text-xs font-bold text-black/50">{label}</span></div>}
function Rule({title,body}:{title:string;body:string}){return <article className="rounded-[24px] border border-black/10 bg-[#fffdf8] p-5"><h3 className="font-black">{title}</h3><p className="mt-2 text-sm leading-6 text-black/55">{body}</p></article>}
