import Link from "next/link";
import {ArrowUpRight,Clock3,MapPin} from "lucide-react";
import type {Task} from "@/lib/data";
export function TaskCard({task}:{task:Task}){return <article className="rounded-3xl border border-black/5 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-soft"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold text-forest">{task.business}</p><h3 className="mt-1 text-xl font-black leading-tight">{task.title}</h3></div><span className="rounded-full bg-leaf/30 px-3 py-1 text-sm font-black">R{task.payment}</span></div><p className="mt-3 text-sm leading-6 text-black/65">{task.summary}</p><div className="mt-4 flex flex-wrap gap-2">{task.skills.map(skill=><span key={skill} className="rounded-full bg-sand px-3 py-1 text-xs font-semibold">{skill}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-black/5 pt-4 text-sm"><div className="flex gap-4 text-black/60"><span className="flex items-center gap-1"><Clock3 size={15}/>{task.duration}</span><span className="flex items-center gap-1"><MapPin size={15}/>{task.location}</span></div><Link className="focus-ring rounded-full bg-ink p-2 text-white" href={`/tasks/${task.id}`}><ArrowUpRight size={18}/><span className="sr-only">View task</span></Link></div></article>}
